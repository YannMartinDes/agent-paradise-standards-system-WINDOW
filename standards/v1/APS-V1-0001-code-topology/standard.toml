schema = "aps.standard/v1"

[standard]
id = "APS-V1-0001"
name = "Code Topology and Coupling Analysis"
slug = "code-topology"
version = "0.2.0"
category = "technical"
status = "active"

[aps]
aps_major = "v1"
backwards_compatible_major_required = true

[ownership]
maintainers = ["AgentParadise"]

# Promotion metadata
[promotion]
promoted_from = "EXP-V1-0001"
promoted_at = "2026-03-10"

[dependencies]
[[dependencies.allowed_external]]
crate = "streaming-iterator"
rationale = "Required by tree-sitter API for traversing syntax tree nodes"

[[dependencies.allowed_external]]
crate = "syn"
rationale = "Optional dep behind RS01 feature: parses Rust source for the merged LANG01 adapter (ADR-0002)"

[[dependencies.allowed_external]]
crate = "quote"
rationale = "Optional dep behind RS01 feature: token handling for the merged LANG01 adapter (ADR-0002)"

[[dependencies.allowed_external]]
crate = "proc-macro2"
rationale = "Optional dep behind RS01 feature: token handling for the merged LANG01 adapter (ADR-0002)"

[[dependencies.allowed_external]]
crate = "chrono"
rationale = "Optional dep behind VZ01 feature: timestamps for the merged VIZ01 dashboard generator (ADR-0002)"
